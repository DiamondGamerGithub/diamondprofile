# Desktop entry

The live desktop page still starts from the root `index.html` because GitHub Pages expects that file at the repository root.

Use this folder for desktop-only notes or future desktop-specific files. Keep the root `index.html` in place unless the deployment setup is changed.
